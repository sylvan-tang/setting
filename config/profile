# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# Alias definitions.
# You may want to put all your additions into a separate file like
# ~/.bash_aliases, instead of adding them here directly.
# See /usr/share/doc/bash-doc/examples in the bash-doc package.

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

#   Change Prompt
#   ------------------------------------------------------------
parse_git_branch() {
    git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/ (\1)/'
}

export PATH="$HOME/.cargo/bin:$PATH"
jdkPath=`ls /Library/Java/JavaVirtualMachines | grep jdk | cut -d ' ' -f 1`
export JAVA_HOME=/Library/Java/JavaVirtualMachines/${jdkPath}/Contents/Home
export PATH="/usr/local/opt/sbt@0.13/bin:$PATH"
export PATH="$HOME/Library/Python/3.7/bin:$PATH"

# Go development
export GOPATH="${HOME}/.go"
export GOROOT="$(brew --prefix golang)/libexec"
export PATH="$PATH:${GOPATH}/bin:${GOROOT}/bin"cd
test -d "${GOPATH}" || mkdir "${GOPATH}"
test -d "${GOPATH}/src/github.com" || mkdir -p "${GOPATH}/src/github.com"

python -c'from datetime import date, datetime; today=datetime.now(); print("Time for testing: %s days" % (date(2020, 5, 1) - date(today.year, today.month, today.day)).days)'
python -c'from datetime import date, datetime; today=datetime.now(); print("Time for Master: %s days" % (date(2020, 9, 1) - date(today.year, today.month, today.day)).days)'

sh "${HOME}/.start-for-work.sh"
cd "${HOME}/codes"
